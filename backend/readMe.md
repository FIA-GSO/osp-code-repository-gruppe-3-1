Setup:

python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt


Für DB:

.env anlegen:
    DB_USER=root
    DB_PASSWORD=secret
    DB_HOST=localhost
    DB_NAME=anmeldetoolmarketplace
